# +PraTi - Lista de Exercícios - 01

__Com Node.js:__
```bash
Instale o pacote prompt-sync:
npm install prompt-sync
```

__Nos arquivos dos exercicios, inicie o prompt-sync:__
```bash
const prompt = require('prompt-sync')();
```
